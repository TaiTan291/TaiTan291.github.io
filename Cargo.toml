[package]
name = "githubio"
version = "0.1.0"
edition = "2024"

[dependencies]
console_error_panic_hook = "0.1.7"
leptos = { version = "0.8.19", features = ["csr"] }
wasm-bindgen = "0.2"

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
